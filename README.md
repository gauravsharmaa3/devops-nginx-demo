# Devops NGINX demo

## Intro

This project helps you to install nginx on linux and them show a demo page.

## Steps

sudo apt-get update

sudo apt install nginx -y

sudo systemctl start nginx

sudo systemctl enable nginx


Webpage is kept at /var/www/html
